#+TITLE: nelisp-emacs Doc 31 -- nemacs-next editor product plan
#+AUTHOR: nelisp-emacs
#+DATE: 2026-06-25
#+STATUS: DRAFT
#+DEPENDS_ON: Doc 17 library-first reuse strategy, Doc 21 APP/GUI semantic definition inventory, Doc 25 library consumer contract, Doc 30 source-of-truth decision

* 0. Purpose

This document starts the =nemacs-next= product line: a new Emacs-like text
editor that reuses the current =nelisp-emacs= library成果 but does not
inherit the current GUI file bridge as its long-term architecture.

The goal is not to fork Emacs C behavior again inside frontend code.  The
goal is to build a daily editor whose GUI is modern and replaceable while
Emacs package compatibility remains owned by the NeLisp / =nelisp-emacs=
runtime session.

* 1. Decision

Build =nemacs-next= as a separate app consumer under:

#+begin_src text
apps/nemacs-next/
  lisp/       product/session contract and app-local assembly
  protocol/   frontend <-> editor protocol notes
  scripts/    focused app smoke checks
#+end_src

=nelisp-emacs= remains the reusable library substrate.  The existing
=gui/= bridge and GTK frontend remain reference and validation assets, but
they are not the desired product frontend.

* 2. Architecture

#+begin_src text
Emacs packages / user init
        |
        v
persistent NeLisp editor session
        |
        v
nelisp-emacs libraries
  foundation / buffer-core / editing / io / special-buffers / core
        |
        v
nemacs-next app protocol
        |
        v
new GUI frontend
  renderer / input / IME / clipboard / menu / tabs / native shell
#+end_src

The persistent session is mandatory.  Per-key process startup and
per-key file polling are explicitly rejected for the product path.

* 3. Ownership

| Area | Owner |
|------+-------|
| Buffer mutation, point, mark, undo | =buffer-core= / =editing= |
| Keymap lookup and command dispatch | =core= command-loop/keymap |
| Minibuffer and completion semantics | =core= minibuffer |
| File and process semantics | =io= |
| Special buffers | =special-buffers= |
| Package loading and user init policy | =nemacs-next= app session |
| Raw keyboard/mouse/IME decoding | GUI frontend |
| Text shaping, font fallback, compositor integration | GUI frontend |
| Menus, toolbar, tabs, native dialogs | GUI frontend calling app protocol |

Rule: if a feature changes editor state in a way an Emacs package can
observe, it does not belong in GUI code.

* 4. Package Compatibility Strategy

The first compatibility target is pure Elisp package code that expects a
normal Emacs global API surface.  GUI compatibility alone is insufficient;
packages observe buffers, variables, hooks, keymaps, minibuffer, process
objects, faces, and timers.

Initial support classes:

| Class | Target |
|-------+--------|
| Pure major/minor modes | early |
| Theme and face packages | early after face protocol |
| Font-lock packages | early after syntax/font-lock lazy surface |
| Completion UI packages | medium, after minibuffer protocol |
| Project/search/xref packages | medium |
| Dired-like file packages | medium |
| Process-heavy packages | later |
| Magit, eglot, LSP clients | later, after process/timer/file-notify maturity |

Package compatibility must be measured by package smoke suites, not by GUI
appearance.

* 5. Frontend Strategy

The new frontend should be built from scratch around a stable protocol.
The current file bridge taught useful lessons, but it should not be the
product transport because it couples latency to filesystem round trips and
per-event runtime startup costs.

Frontend responsibilities:

- retain a local visual model for fast painting;
- send input events and command requests to the persistent session;
- render session-provided buffer snapshots, face spans, cursor state,
  minibuffer state, diagnostics, menus, and echo messages;
- own native OS integrations such as clipboard, IME, window chrome,
  font discovery, and drag/drop;
- never decide editor semantics that packages can observe.

* 6. Protocol V0 Shape

The V0 protocol is message oriented and persistent.

Client to session:

| Message | Meaning |
|---------+---------|
| =hello= | negotiate protocol and capabilities |
| =input= | raw key/mouse/IME event after frontend decoding |
| =command= | named command request, e.g. menu or toolbar |
| =resize= | viewport dimensions changed |
| =open= | OS-level file-open request |
| =clipboard= | clipboard payload/result |
| =shutdown= | graceful app exit |

Session to client:

| Message | Meaning |
|---------+---------|
| =snapshot= | buffer text viewport, point, mark, faces, mode-line |
| =delta= | incremental changes after a command |
| =minibuffer= | prompt, contents, candidates, active command |
| =echo= | message/echo area update |
| =menu= | menu/toolbar/tab model |
| =request= | OS integration request such as clipboard read |
| =error= | structured runtime or command error |

The protocol may start as newline-delimited JSON for debuggability.  It
can move to a binary transport later without changing semantic ownership.

* 7. Milestones

** M0 Product boundary

- Add =apps/nemacs-next/= scaffold.
- Add contract module that declares required =nelisp-emacs= packages and
  frontend-owned surfaces.
- Add smoke that proves host Emacs can load the consumer contract without
  app/bootstrap or legacy GUI.

** M1 Persistent session proof

- Start one NeLisp process and keep it alive.
- Load =nelisp-emacs= facade in that session.
- Execute basic commands without restarting the runtime.
- Return a text snapshot after each command.

** M2 Editing loop

- Self-insert, movement, newline, delete, undo, yank.
- Single buffer plus scratch/messages.
- Latency target: common edit commands under 16ms session-side on warm
  state, excluding frontend painting.

** M3 Minibuffer/file loop

- =find-file=, =save-buffer=, =switch-to-buffer=, =kill-buffer=.
- Completion protocol for minibuffer candidates.
- No GUI-local file command semantics.

** M4 Modern GUI shell

- Native window, renderer, keyboard input, IME, clipboard.
- Buffer viewport and modeline rendering.
- Menus/toolbar call protocol commands, not GUI command copies.

** M5 Package smoke

- Load selected pure Elisp package fixtures.
- Verify mode activation, keymaps, hooks, faces, and buffer edits.
- Track unsupported APIs as package-compat debt.

* 8. Gates

Focused gates for =nemacs-next= should start smaller than the existing full
library gate:

- host load smoke for =apps/nemacs-next/lisp/nemacs-next.el=;
- protocol schema sanity smoke;
- persistent session command smoke once the session exists;
- package compatibility smoke for selected package fixtures;
- frontend latency smoke after the renderer exists.

Library-surface changes still use the normal repository gates from Doc 12
and AGENTS.md.  App-only protocol or documentation changes should not
force a full package extraction gate unless they touch library manifests
or reusable APIs.

* 9. Current Non-goals

- Do not rewrite existing =src/nemacs-gui-file-bridge-runtime.el= as the
  product session.
- Do not put command behavior into the new GUI.
- Do not claim full Emacs package compatibility before package smokes
  exist.
- Do not move =src/= to =packages/= as part of this product start; Doc 30
  keeps =src-primary= for now.

* 10. First Implementation Step

The first committed implementation should be intentionally small:

- =apps/nemacs-next/lisp/nemacs-next.el= declares product contract data;
- =apps/nemacs-next/scripts/smoke.sh= checks that the contract can be
  loaded as a consumer of =nelisp-emacs=;
- =apps/nemacs-next/scripts/session-smoke.sh= checks that a persistent
  NeLisp REPL can load the app contract and evaluate more than one form in
  the same session;
- =apps/nemacs-next/scripts/process-smoke.sh= checks that one persistent
  NeLisp REPL process can emit newline-delimited JSON responses for several
  sequential protocol messages;
- no editor command semantics are implemented in app/frontend glue.

This creates a reviewable boundary before any new GUI code is added.

* 11. Progress Log

This section tracks the milestone ladder in Section 7 against what has
actually landed in =apps/nemacs-next/=, since this document does not carry
a separate numbered increment ladder of its own.

| Date | Milestone | Status | Evidence |
|------+-----------+--------+----------|
| 2026-06-25 | M0 Product boundary | done | =lisp/nemacs-next.el= contract + =scripts/smoke.sh= |
| 2026-06-25 | M1 Persistent session proof | done | =lisp/nemacs-next-session.el= + =lisp/nemacs-next-protocol.el= + =scripts/session-smoke.sh= + =scripts/process-smoke.sh= (=hello=, =create-buffer=, =insert-text=) |
| 2026-07-04 | M2 Editing loop (movement/delete slice) | done | =forward-char=, =backward-char=, =goto-char=, =delete-char= session commands; out-of-range requests return a structured protocol =error= instead of an uncaught signal |
| 2026-07-04 | M2 Editing loop (newline, undo, kill/yank slice) | done | =newline=, =undo=, =kill-region=, =kill-line=, =yank= session commands wired to already-shipped Track E/E.2 substrate (=emacs-undo-*=, =emacs-edit-*-direct=); structured =empty-kill-ring=, =bad-command=, =no-further-undo-information= errors instead of uncaught signals or silent no-ops |
| 2026-07-05 | M3 Minibuffer/file loop | done | =find-file=, =save-buffer=, =switch-to-buffer=, =kill-buffer= session commands; =complete=/=completion= minibuffer candidate payload; =open= delegates to =find-file=; all app smokes cover file/buffer/completion loop plus one standalone org-mode + =org-element-parse-buffer= assertion |
| 2026-07-05 | M4 Modern GUI shell | done | =hello= advertises GUI-shell capabilities; =frame-snapshot= / =render-frame-text= expose buffer viewport + modeline; =resize= / =input= / IME commit produce frame =delta= payloads; =menu= / =toolbar= items route to protocol commands; clipboard read/write produce frontend =request= payloads; =scripts/m4-gui-shell-smoke.sh= renders one frame to plain text |
| -- | M5 Package smoke | open | not yet implemented |

The 2026-07-04 movement/delete increment was picked because M0 and M1
were already complete and M2 (Section 7) was the first milestone with
open work; it also exercises the buffer-core/editing library surface
that the substrate hardening tracked in Doc 33 brought to a stable
state.  All three existing app smokes (=smoke.sh=, =session-smoke.sh=,
=process-smoke.sh=) were extended in place to cover the new commands
plus an out-of-range error path, and all three passed before and after
the change.

A second same-day increment closed the rest of M2 (newline, undo,
kill/yank).  This turned out to be mostly a wiring exercise rather than
new substrate work: =packages/nelisp-emacs-editing/lisp/emacs-undo.el=
(Doc 51 Track E.2) already implements a real per-buffer undo list
(=emacs-undo-undo-direct=, independent of host Emacs's native
=buffer-undo-list=), and
=packages/nelisp-emacs-editing/lisp/emacs-edit-builtins.el= (Track E)
already implements a shared kill-ring with =emacs-edit-kill-region-direct=,
=emacs-edit-kill-line-direct=, and =emacs-edit-yank-direct=.  Both are
loaded transitively through the =emacs-editing= facade group that
=nemacs-next.el= already required, so no substrate/=packages/= changes
were needed -- only =apps/nemacs-next/lisp/nemacs-next-session.el=
wiring:

- =insert-text= and =delete-char= (the M1/M2 commands that mutate the
  buffer through the raw =nelisp-ec-insert= / =nelisp-ec-delete-char=
  primitives) were extended to call =emacs-undo-record-insert= /
  =emacs-undo-record-delete= themselves, since those primitives bypass
  the automatic recording built into unprefixed commands like
  =self-insert-command=.  Skipping this would have made =undo= silently
  not cover the two commands the milestone explicitly asks it to cover.
- Every buffer-mutating command closes its own undo group with
  =emacs-undo-undo-boundary= after completing, so one =undo= command
  reverts exactly the most recent command's edit (verified by a
  newline-then-undo round trip in all three smokes).
- =newline= deliberately does not call the unprefixed =newline= command:
  =emacs-edit-builtins= only installs that polyfill when host Emacs does
  not already provide the name, so under the host-Emacs smoke path
  (=scripts/smoke.sh=) it would silently operate on the host's own
  buffer instead of the =nelisp-ec= session buffer.  =newline= inserts
  through =nelisp-ec-insert= instead -- the same primitive =insert-text=
  already used for a literal =\n= payload -- and is provided as a named
  convenience for frontend Enter-key handling.  See
  =apps/nemacs-next/protocol/protocol-v0.org= for the full rationale.
- =kill-region=/=kill-line=/=yank= call the =-direct= prefixed
  entry points (never the unprefixed =kill-region=/=kill-line=/=yank=
  commands) for the same host-independence reason.

All three app smokes were extended with assertions for every new
command, including the error paths the milestone asked for: =yank= on
an empty kill ring, =kill-region= with missing =:start=/=:end=, and
=undo= on a fresh buffer with no history -- each returns a structured
protocol =error= and the session keeps handling further commands
afterward rather than crashing.  =make test-fast= was not run because
no =packages/= substrate file changed; the three app smokes are the
relevant gate per Section 8.

M3 (Section 7) is now complete.  The app session implements the exact
minibuffer/file loop named by the milestone: =find-file=, =save-buffer=,
=switch-to-buffer=, =kill-buffer=, and completion protocol responses for
minibuffer candidates.  The implementation remains app-thin:
=nemacs-next-session.el= delegates file visit/save to
=emacs-fileio-visit-file-direct= / =emacs-fileio-save-buffer-direct=,
buffer selection and kill to the reusable buffer registry, and candidate
filtering to the minibuffer completion APIs.  Successful commands return
buffer snapshots; missing/invalid file or buffer requests return
structured protocol =error= payloads and leave the session alive.

The M3 smoke coverage extends all three existing app smokes:
=smoke.sh= covers the host-Emacs consumer path, =session-smoke.sh=
covers one persistent NeLisp session, and =process-smoke.sh= covers
newline-delimited JSON responses from one persistent NeLisp process.
The standalone session smoke also opens an Org file through the M3
=find-file= command, activates =org-mode=, and asserts
=org-element-parse-buffer= returns a parse tree.  This is an
editor-on-substrate assertion only; it does not promote Org parsing into
the M3 protocol scope.

M4 is now implemented as the first modern GUI shell protocol slice under
=apps/nemacs-next=.  The exact Section 7 scope was: native window,
renderer, keyboard input, IME, clipboard; buffer viewport and modeline
rendering; menus/toolbar call protocol commands, not GUI command copies.
This commit provides the session/protocol side of that scope: V0 =hello=
advertises ownership/capability rows for those shell surfaces;
=frame-snapshot= returns character-cell viewport rows, cursor position,
modeline, echo area, and the underlying buffer snapshot; =render-frame-text=
turns that frame payload into a plain-text terminal demo; =resize=,
keyboard text input, Enter/Backspace, and IME commit text return =delta=
payloads after delegating buffer mutation to existing shared command
paths; clipboard read/write are frontend OS-integration =request= payloads;
and the menu/toolbar model carries protocol command names such as
=find-file= and =save-buffer= instead of reimplementing command behavior.

The user-facing gap is explicit: this is human-visible and operable at the
protocol/text-render level, but it is not yet a real OS-native window or
hardware clipboard/IME implementation.  Those remain frontend-owned
surfaces for the next GUI integration slice; M5 in Section 7 remains the
package-smoke milestone.

Remaining after M4: M5 package smoke.
