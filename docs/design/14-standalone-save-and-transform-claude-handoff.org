#+TITLE: standalone-save-and-transform Claude Code 引き継ぎ指示書

* 目的

=docs/design/13-standalone-save-and-transform-next.org= の完遂を継続する。

現在の主作業は、=nemacs-gui-file-bridge-run= の post-boundary
command-specific writeback chain を command group ごとに no-op helper 化し、
runtime state reset / writeback 境界を測れる形へ分割すること。

短縮採用ではなく、まず巨大な =equal cmd= chain を安全に分割している段階。
fresh process 前提の =standalone-save-and-transform= を維持し、単純な
session/progn 一括化には戻らないこと。

* 現在地

- branch: =feat/doc142-gate5-nemacs=
- 直近実装 commit: =8cef714 Extract bridge truncate text scale frame writeback chunk=
- 直近の完了 chunk: truncate/text-scale/frame writeback chunk
- 最新 shape:
  - direct transport write: =322 -> 319=
  - =files--write*= helper: =359 -> 355=
  - =equal cmd= tests: =181 -> 176=
  - post-boundary top-level =setq=: =517 -> 510=
  - post-boundary top-level =if=: =228 -> 223=
- 最新 slow gate:
  - =standalone-save-and-transform= pass, =109.419179 sec=

この引き継ぎ書作成時点で、Codex 側の worktree は handoff doc 以外 clean。
Claude Code 側でも対象外の user 変更が見えた場合は勝手に revert / commit しないこと。

* 直近の完了 commit

- =8cef714= truncate/text-scale/frame writeback chunk
- =6ef765f= mark/global/rectangle writeback chunk
- =c74db75= kill/yank writeback chunk
- =f0ba42b= runtime OS detection via uname(2) (別作業。触らない)
- =173d066= indent/newline writeback chunk
- =d0f0076= kmacro writeback chunk
- =df0e283= emoji writeback chunk
- =1b02065= delete/insert writeback chunk
- =5c55117= paragraph/region edit writeback chunk
- =b07a0ef= mark/count/eval writeback chunk
- =7051041= kill/abbrev writeback chunk
- =0e5fa7f= word/sexp/defun motion writeback chunk
- =c722c18= side/window resize writeback chunk
- =dd74550= customize writeback chunk
- =2d6e10d= magit/vc writeback chunk
- =79c4498= org writeback chunk
- =3c8492c= dired writeback chunk

* 次の作業開始点

=src/nemacs-gui-file-bridge-runtime.el= の
=files--bridge-truncate-text-scale-frame-writeback-current-context= 呼び出し直後から再開する。

現時点の次 block:

#+begin_src elisp
(setq cmd (files--bridge-truncate-text-scale-frame-writeback-current-context cmd))
(if (equal cmd "tmm-menubar") ...)
(if (equal cmd "set-selective-display") ...)
(if (equal cmd "revert-buffer-with-coding-system") ...)
(if (if (equal cmd "toggle-input-method") ...) ...)
#+end_src

推奨する次 chunk:

- helper 名: =files--bridge-tmm-menubar-writeback-current-context=
- 対象:
  - =tmm-menubar=
- branch 分類:
  - buf+file+buffer-name+read-only+point+mark+window-start

=set-selective-display= は selective-display+modeline、=revert-buffer-with-coding-system= は
buf+file+point、coding/input-method 群は coding/input state なので、=tmm-menubar= と混ぜず
次以降の別 chunk に回すのが安全。

* 変更手順

1. =src/nemacs-gui-file-bridge-runtime.el= に helper を追加する。
   - 既存 helper と同じ =fset= + =lambda (cmd)= 形式に揃える。
   - helper は最後に =cmd= を返す。
   - 既存の =nl-write-file= / =files--write-transport-*= の順序は変えない。
2. =nemacs-gui-file-bridge-run= 内の =tmm-menubar= block を
   =(setq cmd (files--bridge-tmm-menubar-writeback-current-context cmd))=
   に置き換える。
3. =test/nemacs-gui-file-bridge-runtime-test.el= の末尾付近に専用 ERT を追加する。
   - 既存の =standalone-bridge-truncate-text-scale-frame-writeback-helper= と同じ形式でよい。
   - probe file に =cmd:status= を書く。
   - =/tmp/nemacs-buf=、=/tmp/nemacs-file=、=/tmp/nemacs-buffer-name=、
     =/tmp/nemacs-read-only=、=/tmp/nemacs-point=、=/tmp/nemacs-mark=、
     =/tmp/nemacs-window-start= を確認する。
   - miss path として =forward-char= が =ok= のまま返ることを確認する。
4. =make nemacs-gui-bridge-run-shape= で shape を再計測する。
5. =docs/design/13-standalone-save-and-transform-next.org= に結果を追記する。
   - shape 数値
   - slow gate 秒数
   - helper gate / roundtrip / fast gate が green であること
   - 次の開始点を次 chunk へ送ること
6. commit する。
7. 作業ログを記録する。

* 必須検証

狭い順に実行する。

#+begin_src sh
emacs --batch -L src -L test \
  -l test/nemacs-gui-file-bridge-runtime-test.el \
  --eval '(princ "test-load-ok\n")'

NEMACS_GUI_BRIDGE_GATE_SELECTOR='nemacs-gui-file-bridge-runtime-test/standalone-bridge-tmm-menubar-writeback-helper' \
  make test-nemacs-gui-bridge-gate

make nemacs-gui-bridge-run-shape

NEMACS_GUI_BRIDGE_GATE_SELECTOR='nemacs-gui-file-bridge-runtime-test/standalone-session-bridge-roundtrip' \
  make test-nemacs-gui-bridge-gate

make test-fast

NEMACS_GUI_BRIDGE_SLOW_SELECTOR='nemacs-gui-file-bridge-runtime-test/standalone-save-and-transform' \
  make test-nemacs-gui-bridge-slow

git diff --check
#+end_src

* 今回の完了済み検証

=6ef765f= で mark/global/rectangle chunk、=8cef714= で truncate/text-scale/frame chunk を確認済み。

=8cef714= の検証結果:

- load: green
- helper gate: green
- shape: direct write =319=、=files--write*= =355=、=equal cmd= =176=、
  top-level =setq= =510=、top-level =if= =223=
- roundtrip gate: green, =1.272658 sec=
- fast gate: green, 708 tests, =2.190591 sec=
- slow gate: green, =109.419179 sec=
- =git diff --check=: green

* 守ること

- publish settle を 1ms に戻して短縮したことにしない。
- host 側で session/progn を単純に一括化しない。
- writeback dispatch table / wrapper 型 minimal read path には戻らない。
- command ごとの writeback state を理解せずに fast path を追加しない。
- 対象外の user 変更を revert / commit しない。
- 1 commit 1 chunk を維持する。

* 作業ログ

Codex 側では =mcp__emacs_eval.worklog_add= が =Transport closed= で失敗することがある。
Claude Code 側で anvil-worklog MCP が使えるなら =worklog-add= を優先する。
MCP が使えない場合のみ SQLite fallback で記録する。

worklog に含める内容:

- 実施内容
- 検証コマンドと秒数
- shape 数値
- commit hash
- 次のアクション

* 完了判定

この引き継ぎ時点では =13-standalone-save-and-transform-next.org= は未完了。

完了扱いにしてよいのは、同 doc の要求を requirement-by-requirement に監査し、
未分割の command writeback chain、未記録の検証、未反映の next action が残っていないと
現在状態で証明できた場合だけ。
