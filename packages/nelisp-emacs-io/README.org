#+TITLE: nelisp-emacs-io package scaffold

* Status

Generated experimental scaffold.  Do not edit this copy as the source of truth; edit =src/= and regenerate.

* Load

#+begin_src emacs-lisp
(require 'emacs-io)
#+end_src

* Metadata

- facade-name: =io=
- owner: =IO=
- package-requires: =nelisp-emacs-buffer-core,nelisp-emacs-foundation,nelisp-emacs-text-core=
- member-features: =emacs-fileio-builtins,emacs-process,emacs-process-builtins,emacs-sqlite,emacs-standalone,files-runtime,nelisp-emacs-compat-fileio=
- lazy-features: =emacs-dump,emacs-shell-command,emacs-vc,files-standalone-buffer,image-loader,nemacs-loaddefs=
- host-features: ==
- vendor-package-features: =nelisp-process=
- source-counts: eager=8 lazy=6

* Smoke

Run the package-path smoke target from the repository root:

#+begin_src sh
make nemacs-library-package-path-smoke
#+end_src

* API Policy

Prefer stable prefixed APIs from the facade contract.  Compatibility globals and builtins helpers remain governed by Doc 26.
