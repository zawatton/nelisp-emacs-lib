#+TITLE: nelisp-emacs-buffer-core package scaffold

* Status

Generated experimental scaffold.  Do not edit this copy as the source of truth; edit =src/= and regenerate.

* Load

#+begin_src emacs-lisp
(require 'emacs-buffer-core)
#+end_src

* Metadata

- facade-name: =buffer-core=
- owner: =BUF=
- package-requires: =nelisp-emacs-foundation,nelisp-emacs-text-core=
- member-features: =emacs-buffer,emacs-buffer-builtins,emacs-line-builtins,emacs-search-builtins,nelisp-emacs-compat,nelisp-text-buffer=
- lazy-features: =emacs-bookmark-ui,emacs-replace=
- host-features: ==
- vendor-package-features: ==
- source-counts: eager=7 lazy=2

* Smoke

Run the package-path smoke target from the repository root:

#+begin_src sh
make nemacs-library-package-path-smoke
#+end_src

* API Policy

Prefer stable prefixed APIs from the facade contract.  Compatibility globals and builtins helpers remain governed by Doc 26.
